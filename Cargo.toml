[workspace]
resolver = "2"
members = [
    "crates/motion",
    "crates/gcode-sim",
    "app",
]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"

[profile.release]
opt-level = 3
lto = true

[profile.dev.package."*"]
opt-level = 2
